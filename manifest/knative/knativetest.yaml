apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
  namespace: test
spec:
  selector:
    matchLabels:
      test: test
  replicas: 1
  template:
    metadata:
      labels:
        test: test
    spec:
      containers:
        - image: knative:latest
          imagePullPolicy: IfNotPresent
          name: test
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          volumeMounts:
            - mountPath: /var/run/docker.sock
              name: dockersock
              subPath: docker.sock
      volumes:
        - name: dockersock
          hostPath:
            path: /var/run/
---
apiVersion: v1
kind: Service
metadata:
  name: test
  namespace: test
spec:
  selector:
    app: test
  ports:
    - port: 80
  type: NodePort
